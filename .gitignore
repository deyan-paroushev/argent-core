# Rust / Cargo
target/
**/target/
Cargo.lock
*.rs.bk

# Soroban build output
*.wasm
.soroban/

# Env / secrets (should never be here, but guard anyway)
.env
.env.*
*.pem
*.key

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/
*.tar.gz

# Local reference artifacts, never published
argent-core-v4-summary.pdf
*.pdf

# Publish the V5 test summary; keep other PDFs (e.g. V4) untracked
!docs/argent-core-v5-summary.pdf
